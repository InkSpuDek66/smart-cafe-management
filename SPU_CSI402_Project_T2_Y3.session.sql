-- ============================================================
-- Migration Script — รันหลังจาก SQLQuery_project.sql แล้ว
-- ใช้สำหรับอัปเดต Schema ที่เพิ่มเติมในระหว่างการพัฒนา
-- ============================================================

-- ข้อ 8: เพิ่มคอลัมน์ Notes ในตาราง InventoryLogs
-- ใช้เก็บหมายเหตุ/สาเหตุของการบันทึก Wastage
ALTER TABLE InventoryLogs
    ADD Notes NVARCHAR(500) NULL;
